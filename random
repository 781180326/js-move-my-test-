函数说明
  此函数可实现块元素的 width、height、left、top、opcity等运动
  对于scrolltop、color等变化无能为力，但这些操作都可在此函数上进行更改扩展来实现
  
 函数传参：
 *@param obj:运动对象
 *@param json:运动的目标值，以json格式表示，比如{"width": 300, "height": 300}
 *@param speed:运动速度
 *@param endFn:回调函数，可执行链式操作
 
 函数返回值：
 *@return undefined


  eg:
  
  var oDiv = document.getElementById("div1");
  move(oDiv, {"width":300, "height":300}, 20, function(){
    seccuss();
  });
